import math

a = int(input("Введите a: "))
z_1 = (math.sin(3 * a) + math.sin(7 * a) - math.sin(24 * a)) / (math.cos(a) + 1 - 2 * math.sin(2 * a) * math.sin(2 * a))
print(f"Z_1 = {z_1}")
b = float(input("Введите b: "))
z_2 = 2 * math.acos(33 * b * b)
print(f"Z_2 = {z_2}")
